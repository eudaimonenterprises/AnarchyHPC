---
---
# Debian-specific repository URLs (Apt format)
xcat_core_repo_url: "deb [arch=amd64] https://xcat.org/files/xcat/repos/apt/latest/xcat-core bionic main"
xcat_dep_repo_url: "deb [arch=amd64] https://xcat.org bionic main"

provisioning_required_packages:
  - xCAT-server
  - xCAT-client
  - xCAT-genesis-base-x86_64
  - xCAT-genesis-scripts-x86_64
  - goconserver
  - ipmitool-xcat

# Services managed by the xCAT role
provisioning_enable_services:
  - xcatd

provisioning_start_services:
  - xcatd

# xCAT directory layout
xcat_install_dir: "/opt/xcat"
xcat_config_dir: "/etc/xcat"
xcat_bin: "/opt/xcat/bin"
xcat_sbin: "/opt/xcat/sbin"

# Sub-services used by xCAT (pointers to Infrastructure)
http_service_name: "apache2"
dhcp_service_name: "isc-dhcp-server"
tftp_service_name: "tftpd-hpa"

# Provisioning network definition
provisioning_network:
  name: "provnet"
  device: "eth0"
  ipaddr: "10.141.255.254"
  netmask: "255.255.0.0"
  network: "10.141.0.0"
  broadcast: "10.141.255.255"
  gateway: ""

# xCAT object placeholders
xcat_nodes: []
xcat_osimages: []
xcat_groups: []
