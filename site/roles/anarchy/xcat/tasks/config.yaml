# roles/anarchy/xcat/tasks/config.yaml

- name: Deploy xCAT config
  template:
    src: xcatd.conf.j2
    dest: /etc/xcat/xcatd.conf
  notify: Restart xcatd

# handlers/main.yaml
- name: Restart xcatd
  service:
    name: xcatd
    state: restarted
