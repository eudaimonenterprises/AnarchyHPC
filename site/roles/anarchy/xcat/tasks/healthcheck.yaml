- name: Ensure xcatd service is active
  service:
    name: xcatd
    state: started

- name: Wait for xcatd port
  wait_for:
    host: 127.0.0.1
    port: 3001
    timeout: 10

- name: Verify xCAT API responds
  command: lsdef -t site
  register: xcat_api
  failed_when: xcat_api.rc != 0
