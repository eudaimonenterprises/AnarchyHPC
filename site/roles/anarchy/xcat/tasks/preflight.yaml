- name: Check DNS resolution
  command: "getent hosts {{ mgmt_hostname }}"
  register: dns_check
  failed_when: dns_check.rc != 0

- name: Check DHCP config syntax
  command: makedhcp -n
  register: dhcp_check
  failed_when: dhcp_check.rc != 0

- name: Check xCAT API responds
  command: lsdef -t site
  register: xcat_api
  failed_when: xcat_api.rc != 0
