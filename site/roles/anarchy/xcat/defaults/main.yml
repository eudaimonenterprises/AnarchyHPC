---
# Provisioning role defaults (xCAT version)

provisioning_enabled: true

# xCAT packages (Ubuntu example)
provisioning_required_packages:
  - xcat-server
  - xcat-client
  - xcat-genesis-scripts-x86_64
  - xcat-genesis-base-x86_64

# Services xCAT actually uses
provisioning_enable_services:
  - xcatd

provisioning_start_services:
  - xcatd

# xCAT configuration directories
xcat_install_dir: "/opt/xcat"
xcat_config_dir: "/etc/xcat"
xcat_bin: "/opt/xcat/bin"
xcat_sbin: "/opt/xcat/sbin"

# Network definition (xCAT uses this for networks table)
provisioning_network:
  device: "eth0"
  ipaddr: "10.141.255.254"
  netmask: "255.255.0.0"
  network: "10.141.0.0"
  broadcast: "10.141.255.255"

# xCAT-specific objects
xcat_nodes: []
xcat_osimages: []
xcat_groups: []
